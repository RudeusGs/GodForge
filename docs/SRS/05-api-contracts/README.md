# API Contract Documents

These files are implementation gates, not generated OpenAPI output. Code annotations/generated OpenAPI must remain consistent with them.

Each endpoint specifies actor, permission, request, response, validation, errors, transaction, idempotency, rate limit, audit and test IDs.

M1 contracts:

- `auth.md`
- `organizations.md`
- `projects.md`
